import re

# STRINGS AND CHARACTER KATA

#we first impor rejects to enables us sort the string




def char(str):
    str = 'gvh??4?6hy'
    filter = re.sub([a-z],'',str)
    x = range(1,10)
    n = x + ???[10-x]
        if n:
            return True
        else:
            return False

